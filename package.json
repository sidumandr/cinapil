{
  "name": "cinapil",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend/Cinapil.API && dotnet run --urls http://localhost:5079"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
